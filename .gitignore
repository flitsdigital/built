# Secrets — nooit committen
Built/Secrets.plist

# Xcode
Built.xcodeproj
DerivedData/
xcuserdata/
*.xcuserstate
.DS_Store

# Xcode derived data from local builds
build/

# Supabase CLI — lokale state (projectkoppeling, versiecache)
supabase/.temp/

# Agent-worktrees — wegwerpkopieën van de repo
.claude/worktrees/

# Oefeningplaatjes — eigendom van Gym visual (gymvisual.com), hier alleen voor eigen
# gebruik. Deze repo is publiek, dus ze horen er niet in. Ophalen met:
#   python3 scripts/exercises.py <pad naar exercises-dataset>
ExerciseMedia/
!ExerciseMedia/.gitkeep

__pycache__/
